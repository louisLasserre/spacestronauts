<%= form_with(model: rocket) do |form| %>
  <% if rocket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rocket.errors.count, "error") %> prohibited this rocket from being saved:</h2>

      <ul>
        <% rocket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :company_id, style: "display: block" %>
    <%= form.text_field :company_id %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.text_area :image %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

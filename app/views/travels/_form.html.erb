
<%= simple_form_for travel do |f| %>
  <%= f.input :date, as: :date, html5: true %>
  <%= f.input :price, as: :float %>
  <%= f.association :destination, include_blank: false %>
  <%= f.association :rocket, include_blank: false %>

  <%= f.button :submit %>
<% end %>
